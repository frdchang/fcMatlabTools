%% RESULT EXIST
% If the base workspace has a loaded result image stack, this script 
% will assign the stack to a new variable in the 
% caller's workspace. Associated with the '+', '-', and 'Go' buttons.

evalin('base', ...
[   'if(exist(''RESULT'', ''var'')) ' ...
    '    assignin(''caller'', ''Result'', RESULT); ' ...
    'else ' ...
    '    assignin(''caller'', ''Result'', []); ' ...
    'end'   ]);
